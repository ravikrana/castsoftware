'use strict';

describe('controller: MainController', function() {
  var ctrl, technologyService, $scope;

  beforeEach(module('webApp'));


  it('should call technologyService', function() {
    expect(technologyService.getRepository('python',1).toHaveBeenCalled();
    expect(LanguagesService.getRepository('python',1).oBeGreaterThan(0);
  });


});